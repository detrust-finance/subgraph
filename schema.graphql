type Trust @entity {
  id: ID!
  name: String! # string
  settlor: Bytes! # address
  beneficiary: Bytes! # address
  nextReleaseTime: BigInt!
  timeInterval: BigInt!
  amountPerTimeInterval: BigInt!
  totalAmount: BigInt!
  cumAmount: BigInt!
  releasedAmount: BigInt!
  revocable: Boolean!
}
